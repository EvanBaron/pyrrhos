services:
  pyrrhos:
    build: .
    restart: unless-stopped
    env_file:
      - .env.production
    volumes:
      - ./data:/app/data
    depends_on:
      - pot-provider

  pot-provider:
    image: brainicism/bgutil-ytdlp-pot-provider
    restart: unless-stopped
    ports:
      - "4416:4416"
